
  # Teatro Entradas Web Mockup (Community)

  This is a code bundle for Teatro Entradas Web Mockup (Community). The original project is available at https://www.figma.com/design/u45nwa2wR0STRYnpTL1SSL/Teatro-Entradas-Web-Mockup--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  